{
  "name": "pour-firestore-backup",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore 백업 및 마이그레이션 도구",
  "scripts": {
    "backup:dev": "NODE_ENV=development node -r dotenv/config firestore-backup.js dotenv_config_path=../.env.dev",
    "backup:prod": "NODE_ENV=production node -r dotenv/config firestore-backup.js dotenv_config_path=../.env.prod",
    "migrate": "node migrate-to-new-project.js"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "dotenv": "^16.3.1",
    "firebase-admin": "^12.0.0",
    "uuid": "^13.0.0"
  }
}
